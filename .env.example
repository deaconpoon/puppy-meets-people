# Copy to .env.local — all keys are OPTIONAL.
# Without a key, the app uses the deterministic fallback scorer + seeded
# matches (data/seeded-matches.ts), so the demo always works.

# Live AI scoring (Slice B) — OpenAI, model gpt-4o-mini (TRD §5)
OPENAI_API_KEY=
